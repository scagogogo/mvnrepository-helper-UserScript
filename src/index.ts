/**
 * 应用程序入口文件
 * 
 * 本文件是整个UserScript应用的入口点，负责启动应用程序初始化流程。
 * 使用立即执行的异步函数(IIFE)模式，确保应用初始化过程可以使用await等异步语法。
 * 
 * 执行流程：
 * 1. 导入AppInitializer类，该类负责具体的初始化逻辑
 * 2. 创建并执行异步的立即执行函数
 * 3. 在函数内部调用AppInitializer.init()方法
 * 4. 等待初始化完成
 * 
 * 特点说明：
 * - 使用异步IIFE模式避免在全局作用域中使用await
 * - 将初始化逻辑委托给专门的AppInitializer类，保持入口文件简洁
 * - 没有错误处理逻辑，异常将被传播到浏览器控制台
 * 
 * 注意事项：
 * - 此文件是UserScript执行的起点，由油猴等脚本管理器直接加载
 * - 应用的所有功能模块都通过AppInitializer进行初始化和协调
 * - 在此文件中不应添加复杂的业务逻辑，应保持入口点的简洁性
 */
import AppInitializer from "./init/AppInitializer";

/**
 * 应用程序初始化的立即执行异步函数
 * 
 * 创建一个异步的立即执行函数环境，用于启动应用程序
 * 使用async/await语法确保应用初始化过程的顺序执行
 * 
 * 执行步骤：
 * 1. 调用AppInitializer.init()方法启动应用初始化流程
 * 2. 等待初始化完成
 * 
 * 错误处理：
 * - 此处未实现显式的错误处理
 * - 如有异常，将被传播到全局作用域，可在浏览器控制台查看
 */
(async () => {
    await AppInitializer.init();
})();

